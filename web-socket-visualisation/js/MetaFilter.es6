"use strict";

const MetaFilter = (function () {
    let MetaFilter = class {
        constructor() {

        }
    };

    let pattern = ``;
    MetaFilter._pattern = $(pattern);
    return MetaFilter;
})();
